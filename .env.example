# GitLab Personal Access Token (scopes: api, read_user, write_repository)
export GITLAB_TOKEN="glpat-REPLACE_ME"

# Google Cloud MCP (optional)
# gcloud beta services mcp enable developerknowledge.googleapis.com --project "projects/YOUR_PROJECT_ID"
# claude mcp add google-dev-knowledge --transport http https://developerknowledge.googleapis.com/mcp --header "X-Goog-Api-Key: YOUR_API_KEY"